pipeline{
	agent any
	
	stages{
		stage('Check stage'){
			steps{
				withMaven(maven : 'Mvn'){
				bat 'mvn --version'
				}
			}
		}
		
		stage('Test stage'){
			steps{
				withMaven(maven : 'Mvn'){
				bat 'mvn -f mvn-math/pom.xml install'
				}
			}
		}
		
		stage('Compile stage'){
			steps{
				withMaven(maven : 'Mvn'){
				bat 'mvn -f mvn-math/pom.xml compile'
				}
			}
		}
		
		
	}


}